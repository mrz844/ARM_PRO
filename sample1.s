.global add_value

// تابع add_value که یک مقدار را دریافت کرده و ۵ به آن اضافه می‌کند
add_value:
    add x0, x0, #5        // مقدار ورودی (x0) را با ۵ جمع می‌کند
    ret                    // بازگشت به تابع فراخوانی در زبان C